node_modules
dist
dev-dist
.wrangler
.DS_Store
*.local
.env
.env.*

# TypeScript incremental build info
*.tsbuildinfo

# Local data fixtures / generated test backups (never commit)
macroid-dummy-trends.json
macroid-varying-targets.json
recomp-data-*.json
macroid-backup-*.json

# Local working copy of the exercise catalog (canonical one is public/exercises.json)
exercises-seed.json

# Scratch AI output (routine plans pasted back from an assistant)
gemini-code-*.json
